@model Shop.Mvc.Models.Educations.EducationSetDetailViewModel
@{
    ViewData["Title"] = "تنظیم نمایش خروجی برای کاربر  - پنل مدیریتی ایمن فروش";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/admin">پنل مدیریتی</a></li>
        <li class="breadcrumb-item active" aria-current="page">تنظیم نمایش خروجی برای کاربر</li>
    </ol>
</nav>

<div class="panel panel-primary">
    <div class="panel-heading">تنظیم نمایش خروجی برای کاربر</div>
    <div class="panel-body">
        <form method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <fieldset style="margin-top:15px">
                <legend>تنظیم نمایش خروجی برای کاربر </legend>
                <textarea asp-for="Description" id="editor"></textarea>
            </fieldset>
            <div class="form-group" style="margin-top:15px">
                <button type="submit" class="ladda-button btn btn-primary" data-style="zoom-in">
                    <span class="ladda-label">ثبت اطلاعات</span>
                </button>
            </div>
        </form>
    </div>
</div>

<script>
    debugger
    initSample();
    if (CKEDITOR.config) {
        CKEDITOR.config.height = 400;
        CKEDITOR.config.resize_enabled = true;
        CKEDITOR.config.contentsLangDirection = 'rtl';
    }

    //$(function () {
    //    $('.editor').each(function (e) {
    //        CKEDITOR.replace(this.id, { customConfig: '/jblog/ckeditor/config_Large.js' });
    //    });
    //})
</script>